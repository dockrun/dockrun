language: bash

services:
  - docker

install:
- sudo make install

script:



jobs:
  include:
    - stage: Test
      script:
      -  dockrun wget https://dockrun.github.io
    - # stage name not required, will continue to use `Test`
      script:
      - sudo ./src/test/dockrun-uninstall-test
